# CLAUDE.md - Rebuilt26 Project Snapshot

This file is project-specific context for `Rebuilt26`. It assumes the reader has already read `agents.md`. Paths are relative to the repository root unless noted.

## 1. ROBOT IDENTITY & GAME CONTEXT

- Robot name / nickname: **Chuck Norris**. Evidence: AdvantageScope asset config names the robot `"Chuck Norris"` (`AScope Assets/Robot_Chuck Norris/config.json:2`), and CAD/model assets live under `ChuckNorrisModels/`.
- Season: **2026 robot code project**. Evidence: project name `Rebuilt26`, generated swerve constants say "Generated by the 2026 Tuner X Swerve Project Generator" (`src/main/java/frc/robot/generated/TunerConstants.java:16`), and GradleRIO is configured in `build.gradle:3`.
- FRC game name: **TODO: confirm**. No README, game-name constant, or docs file in this repo names the official game.
- What this robot is built to do: This codebase is for a swerve robot that intakes "fuel", stores it in a simulated capacity, and shoots through **two turret assemblies** at a field hub target. It has hub targeting, ferry targeting from the neutral zone, a pivoting intake, hotdog/indexer feed motors, dual turret azimuth/flywheel/hood control, two Limelight pose cameras, PathPlanner autos, and a fuel/hub/trench simulation (`Constants.java:250`, `RobotContainer.java:147`, `RobotContainer.java:172`, `FuelSimulationController.java:31`, `FuelSim.java:830`). The field vocabulary in code is "hub", "fuel", "neutral zone", "trench", "ferry", and "stoot"; confirm the real game mapping before changing field assumptions.
- Driver/operator control scheme:
  - Competition mode uses driver controller port 0 and operator controller port 1 (`RobotContainer.java:79`, `RobotContainer.java:80`).
  - Driver handles swerve driving, zero-heading, and hub-facing drive lock (`RobotContainer.java:314`, `RobotContainer.java:320`, `RobotContainer.java:386`).
  - Operator handles intake, retract, shoot/feed, unjam/reverse, and turret mode toggle (`RobotContainer.java:273`, `RobotContainer.java:298`, `RobotContainer.java:300`, `RobotContainer.java:333`, `RobotContainer.java:348`).
  - "CRAZY" binding mode moves those controls to the driver controller only (`CrazyModeBindings.java:28`, `CrazyModeBindings.java:36`).
- Known competition events targeted:
  - `Gotham` is referenced by recent commit messages and auto names: `aa3065e comp changes from gotham`, `eae1000 Renamed the auto...`, `RIGHT GOTHAM DOUBLE.auto`, `PLAYOFF LEFT BATMAN.auto`.
  - Other event names: **TODO: confirm**.

## 2. PHYSICAL ROBOT ARCHITECTURE

### Drivetrain

- Type: four-module swerve drive, CTRE Tuner-generated constants, Phoenix high-frequency odometry thread (`TunerConstants.java:16`, `Drive.java:53`, `Drive.java:125`).
- Module hardware in code: TalonFX drive motor, TalonFX steer motor, CANcoder absolute encoder per module (`ModuleIOTalonFX.java:40`, `ModuleIOTalonFX.java:51`).
- Motor model assumed by code: Kraken X60 FOC in PathPlanner/sim config (`Drive.java:77`, `ModuleIOSim.java:37`).
- Wheel radius: 2 inches, so 4 inch diameter (`TunerConstants.java:112`).
- Drive ratio: `6.746031746031747` (`TunerConstants.java:110`).
- Steer ratio: `21.428571428571427` (`TunerConstants.java:111`).
- Coupling ratio: `3.5714285714285716` (`TunerConstants.java:108`).
- Module locations used by code:
  - FL `(10.875 in, 11.375 in)` (`TunerConstants.java:166`, `TunerConstants.java:167`)
  - FR `(10.875 in, -11.375 in)` (`TunerConstants.java:177`, `TunerConstants.java:178`)
  - BL `(-10.875 in, 11.375 in)` (`TunerConstants.java:188`, `TunerConstants.java:189`)
  - BR `(-10.875 in, -11.375 in)` (`TunerConstants.java:199`, `TunerConstants.java:200`)
- Derived from the above: wheelbase `21.75 in / 0.55245 m`, trackwidth `22.75 in / 0.57785 m`, drive base radius about `0.39972 m`.
- TODO: confirm module brand/model such as MK4i/L2; not named anywhere in code.

### Mechanisms

| Mechanism | Purpose | Motors / actuators | Sensors | Range / limits |
|---|---|---|---|---|
| Intake | Pivot down/up and run roller to collect fuel | TalonFX roller ID 5; TalonFX pivot ID 12 (`Constants.java:159`) | TalonFX integrated position/velocity only (`Intake.java:78`, `Intake.java:82`) | Pivot `upPos = 0`, `downPos = 12.75` rotations/control units; TODO says update values (`Constants.java:163`) |
| Indexer / hotdog | Move stored fuel into shooter or reverse for unjam | TalonFX hotdog ID 4; TalonFX indexer ID 14 (`Constants.java:208`) | TalonFX velocity feedback (`Indexer.java:47`, `Indexer.java:69`) | Velocity setpoints: indexer 55 RPS, hotdog 70 RPS, unjam hotdog -50 RPS (`Constants.java:212`) |
| Left turret | Aim and shoot from robot-left turret position | Azimuth TalonFX ID 19; flywheel TalonFX ID 3; follower TalonFX ID 2; hood servo channel 1 in RobotContainer because `HOOD_SERVO_CHANNEL_2` is passed to `turret1` (`RobotContainer.java:147`) | CANdi ID 45 S1 limit input for zeroing (`Turret.java:54`, `Turret.java:151`) | Azimuth control range -380 to 1 deg plus 10 deg soft-limit margin; hood 13 to 29.85 deg; flywheel 0 to 100 RPS (`Constants.java:75`, `Constants.java:81`) |
| Right turret | Aim and shoot from robot-right turret position | Azimuth TalonFX ID 17; flywheel TalonFX ID 16; follower TalonFX ID 18; hood servo channel 0 (`RobotContainer.java:158`) | CANdi ID 45 S2 limit input for zeroing (`Turret.java:54`, `Turret.java:151`) | Azimuth control range -365 to 10 deg plus 10 deg soft-limit margin; same hood/flywheel limits (`Constants.java:78`) |
| Hood servos | Set shooter hood angle for both turrets | REV ServoHub CAN ID 63; channel 0 and 1 (`Constants.java:87`, `Hood.java:52`) | No position sensor in code; software stores last setpoint (`Hood.java:17`) | Pulse range 500/1500/2500 us, one channel inverts pulse direction (`Constants.java:92`, `Hood.java:22`) |
| Vision | Pose estimation and target angles | Two Limelights on real robot, PhotonVision sim cameras in sim (`RobotContainer.java:105`, `RobotContainer.java:123`) | AprilTag observations, `tx`, `ty`, MegaTag arrays (`VisionIOLimelight.java:45`) | Rejection: no tags, high ambiguity, Z error > 0.75 m, outside field (`Vision.java:99`) |

### CAN ID Map

| Device name | CAN ID | Bus | Type |
|---|---:|---|---|
| Front-left drive | 9 | CANivore | TalonFX |
| Front-left steer | 8 | CANivore | TalonFX |
| Front-left encoder | 44 | CANivore | CANcoder |
| Front-right drive | 6 | CANivore | TalonFX |
| Front-right steer | 7 | CANivore | TalonFX |
| Front-right encoder | 41 | CANivore | CANcoder |
| Back-left drive | 0 | CANivore | TalonFX |
| Back-left steer | 1 | CANivore | TalonFX |
| Back-left encoder | 43 | CANivore | CANcoder |
| Back-right drive | 10 | CANivore | TalonFX |
| Back-right steer | 11 | CANivore | TalonFX |
| Back-right encoder | 42 | CANivore | CANcoder |
| Pigeon 2 | 46 | CANivore | Pigeon2 |
| Intake roller | 5 | CANivore | TalonFX |
| Intake pivot | 12 | CANivore | TalonFX |
| Hotdog | 4 | CANivore | TalonFX |
| Indexer | 14 | CANivore | TalonFX |
| Left turret azimuth | 19 | CANivore | TalonFX |
| Left turret flywheel leader | 3 | CANivore | TalonFX |
| Left turret flywheel follower | 2 | CANivore | TalonFX |
| Right turret azimuth | 17 | CANivore | TalonFX |
| Right turret flywheel leader | 16 | CANivore | TalonFX |
| Right turret flywheel follower | 18 | CANivore | TalonFX |
| Turret zeroing CANdi | 45 | CANivore | CANdi |
| Hood ServoHub | 63 | default CAN bus in `new ServoHub(63)` | REV ServoHub |

CANivore is defined in `Constants.java:38`; TunerConstants also defines CANivore with hoot logging (`TunerConstants.java:100`).

### Other Ports And Hardware

- DIO: None found in project source. Turret zeroing uses CANdi S1/S2, not roboRIO DIO (`Turret.java:151`).
- PWM: None found.
- Analog: None found.
- PDH/PDP channel assignments: None found.
- Pneumatics: None found.
- Cameras:
  - `limelight-back`: robot-to-camera transform `(-0.3118612, 0.2235454, 0.2118868)` m, pitch -30 deg, yaw 180 deg (`VisionConstants.java:27`).
  - `limelight-right`: code transform `(13.372 in, -2.875 in, 7.48 in)`, pitch -20 deg, yaw -90 deg (`VisionConstants.java:34`).
  - AdvantageScope model config also lists Limelight-Right at `(-0.073025, -0.3396488, 0.189992)` m (`AScope Assets/Robot_Chuck Norris/config.json:60`), which does not match `VisionConstants`. TODO: confirm which is authoritative before editing camera poses.
- Non-obvious physical constraints:
  - Turret azimuth setpoints are wrapped/clamped inside each turret's min/max range; do not bypass `selectSafeSetpointDegrees` (`Turret.java:162`).
  - Turret zeroing only happens while disabled, based on a recent commit and current code (`Turret.java:143`; commit `fc5d170`).
  - Hood position is open-loop by pulse width; no feedback object exists (`Hood.java:26`, `Hood.java:33`).

## 3. CODE STRUCTURE - ACTUAL FILE TREE

```text
src/main/java/frc/robot/
  BuildConstants.java - generated build metadata used by AdvantageKit metadata logging.
  Constants.java - robot IDs, tuning values, field coordinates, and fuel sim constants.
  Main.java - program entrypoint.
  Robot.java - LoggedRobot lifecycle, logging setup, brownout voltage, auto/sim scheduling.
  RobotContainer.java - subsystem construction, real/sim IO selection, NamedCommands, autos, bindings, defaults.
  commands/
    DriveCommands.java - joystick drive, angle-lock drive, drive characterization factories.
    IntakeCommand.java - held intake command that extends pivot and runs roller based on drive speed.
  controls/
    CrazyModeBindings.java - alternate one-controller binding map selected from dashboard while disabled.
  generated/
    TunerConstants.java - CTRE Tuner-generated swerve constants and initial drive/steer configs.
  sim/
    FuelLaunchCalculator.java - converts shot table/hood/flywheel values into simulated launch states.
    FuelSim.java - field fuel physics, hub scoring, collision, intake registration.
    FuelSimCommand.java - perpetual command that runs fuel simulation, including while disabled.
    FuelSimulationController.java - bridges robot subsystem state into FuelSim.
  subsystems/
    drive/
      Drive.java - swerve subsystem, pose estimator, AutoBuilder config, SysId hooks.
      GyroIO.java - AdvantageKit gyro IO contract.
      GyroIOPigeon2.java - real Pigeon2 IO.
      Module.java - swerve module wrapper around ModuleIO and Tuner constants.
      ModuleIO.java - AdvantageKit module IO contract.
      ModuleIOSim.java - DCMotorSim module IO.
      ModuleIOTalonFX.java - real TalonFX/CANcoder module IO.
      PhoenixOdometryThread.java - Phoenix status-signal odometry sampling thread.
    indexer/
      Indexer.java - hotdog/indexer TalonFX velocity subsystem.
    intake/
      Intake.java - intake roller and pivot TalonFX subsystem.
    turret/
      GetAdjustedShot.java - shot table interpolation and moving-shot compensation.
      Turret.java - aggregate turret subsystem around azimuth, hood, flywheels, zeroing.
      TurretTargeting.java - default/ferry targeting command.
      TurretVisualization.java - AdvantageScope component pose logging.
      turret_base/
        Azimuth.java - azimuth TalonFX Motion Magic wrapper.
        Flywheels.java - flywheel leader/follower velocity wrapper.
        Hood.java - REV ServoHub hood wrapper.
    vision/
      Vision.java - multi-camera pose filtering and pose-estimator injection.
      VisionConstants.java - AprilTag layout, camera transforms, vision std-dev constants.
      VisionIO.java - AdvantageKit vision IO contract.
      VisionIOLimelight.java - real Limelight NetworkTables IO.
      VisionIOPhotonVision.java - real PhotonVision IO implementation, not used on real robot currently.
      VisionIOPhotonVisionSim.java - PhotonVision simulation wrapper.
  util/
    AllianceFlipUtil.java - dynamic alliance mirroring helper.
    Distancer.java - deploy JSON shot-table loader/interpolator data model.
    Elastic.java - Elastic dashboard notification helper.
    Field2dPublisher.java - SmartDashboard Field2d pose publisher.
    LocalADStarAK.java - AdvantageKit-loggable PathPlanner local AD* adapter.
    PhoenixUtil.java - retry helper for CTRE config calls.
    Zones.java - predictive rectangular field-zone utilities.
```

Hot files by recent commit history: `RobotContainer.java`, `Constants.java`, `TunerConstants.java`, PathPlanner autos/paths, `Drive.java`, turret files, intake/indexer files, and sim files (`git log --name-status`, commits `aa3065e` through `b0f98f6`).

Stable or generated/vendor-derived files: `Main.java`, `BuildConstants.java`, `TunerConstants.java` should be treated carefully because it is generated (`TunerConstants.java:16`), `PhoenixOdometryThread.java` and `LocalADStarAK.java` are template/adapter style files with upstream comments (`LocalADStarAK.java:25`).

Unusual project choices:

- Two separate `Turret` subsystem instances share one static targeting-mode lock flag (`Turret.java:34`, `Turret.java:95`).
- The project has two control maps, "Competition" and "CRAZY", swapped through an `EventLoop` chooser only while disabled (`RobotContainer.java:64`, `RobotContainer.java:268`).
- Real robot vision uses Limelight IO, while sim uses PhotonVision sim with the same camera names (`RobotContainer.java:105`, `RobotContainer.java:123`).
- `src/main/deploy/pathplanner/autos/*.auto` files are version `"2025.0"` even though this is a 2026 codebase. TODO: confirm if PathPlanner 2026 intentionally preserves that file format.

## 4. SUBSYSTEMS

### Drive - `src/main/java/frc/robot/subsystems/drive/Drive.java`

- Hardware owned indirectly: 4 modules created from `ModuleIO` implementations plus gyro IO (`Drive.java:106`). Real mode passes `GyroIOPigeon2` and four `ModuleIOTalonFX` instances (`RobotContainer.java:93`).
- Public action/method surface:
  - `runVelocity(ChassisSpeeds)` - drive closed-loop module states from chassis speeds (`Drive.java:234`).
  - `runCharacterization(double)` - straight-line module characterization output (`Drive.java:254`).
  - `runRotationCharacterization(double)` - spin-characterization module headings (`Drive.java:261`).
  - `stop()` - zero chassis speeds (`Drive.java:270`).
  - `stopWithX()` - reset kinematic headings to X and stop (`Drive.java:278`).
  - `sysId...` methods - translation/rotation SysId commands (`Drive.java:287`).
  - `getPose()`, `getRotation()`, `getRobotRelativeSpeeds()`, `getFieldRelativeSpeeds()` - odometry accessors (`Drive.java:368`).
  - `setPose(Pose2d)` - reset pose estimator (`Drive.java:390`).
  - `addVisionMeasurement(...)` - inject accepted vision pose (`Drive.java:395`).
  - `getMaxLinearSpeedMetersPerSec()` and `getMaxAngularSpeedRadPerSec()` - teleop speed caps (`Drive.java:403`).
- Default command: `DriveCommands.joystickDrive(...)` with driver left stick translation and right X rotation (`RobotContainer.java:386`).
- Control modes: swerve velocity control through module IO; PathPlanner AutoBuilder uses `runVelocity` (`Drive.java:128`).
- Gotchas:
  - Teleop max speed is reduced to `2.5 m/s` when intake or feed is active (`Drive.java:404`, `Constants.java:55`).
  - PathPlanner config mass/MOI in code is `74.088 kg / 6.883`, but `settings.json` says `66.0 / 8.45` (`Drive.java:65`, `src/main/deploy/pathplanner/settings.json:17`). TODO: confirm which reflects the real robot.
  - `AutoBuilder.configure` flips paths on red alliance (`Drive.java:137`); `AllianceFlipUtil` also flips driver field-relative frame and hub/ferry targets dynamically (`AllianceFlipUtil.java:51`).
- Current PID/feedforward:
  - PathPlanner drive/rotation PID: `5.0, 0.0, 0.0` for both (`Drive.java:134`).
  - Drive motor gains: kP `0.1`, kS `0.2225`, kV `0.84381` (`TunerConstants.java:34`).
  - Steer gains: kP `100`, kD `0.5`, kS `0.1`, kV `2.66` (`TunerConstants.java:23`).
- Limits/conversions:
  - Drive stator 80 A / supply 30 A; steer stator 40 A / supply 20 A (`TunerConstants.java:62`, `TunerConstants.java:73`).
  - Max speed at 12 V: `4.58 m/s` (`TunerConstants.java:104`).
  - Max angular speed from current speed divided by drive base radius (`Drive.java:413`).

### Intake - `src/main/java/frc/robot/subsystems/intake/Intake.java`

- Hardware: TalonFX intake roller ID 5 and pivot ID 12 on CANivore (`Intake.java:29`, `Constants.java:159`).
- Public surface:
  - `setIntakeSpeed(double)` - velocity-control roller and update `isIntaking` (`Intake.java:36`).
  - `reverseIntake()` - run roller at `unjamSpeed` (`Intake.java:42`).
  - `startIntake()` / `stopIntake()` (`Intake.java:46`, `Intake.java:51`).
  - `setPivotPosition(double)`, `extend()`, `retract()` (`Intake.java:65`, `Intake.java:70`, `Intake.java:74`).
  - `isAcceptablePosition(double)`, `isIntaking()`, `isPivotCommandedDown()` (`Intake.java:78`, `Intake.java:56`, `Intake.java:101`).
  - Command factories: `extendCommand`, `retractCommand`, `onIntake`, `zeroIntake`, `offIntake` (`Intake.java:105`).
- Default command: none.
- Control modes: roller uses `VelocityVoltage`; pivot uses `MotionMagicVoltage` (`Intake.java:21`).
- Current gains and values:
  - Roller: kP `0.35`, kV `0.105`, speed `70`, unjam `-50` (`Constants.java:167`).
  - Pivot: kP `15`, kS `0.5`, kV `0.12`, kG `1`, cruise `60`, accel `120` (`Constants.java:173`).
- Limits/current:
  - Pivot stator 40 A, supply 20 A, lower limit 60 for 1 s (`Constants.java:181`).
  - Intake stator 60 A, supply 30 A, lower limit 37.5 for 0.2 s (`Constants.java:195`).
- Gotchas: positions are marked "UPDATE ME" in constants (`Constants.java:163`).

### Indexer - `src/main/java/frc/robot/subsystems/indexer/Indexer.java`

- Hardware: hotdog TalonFX ID 4 and indexer TalonFX ID 14 on CANivore (`Indexer.java:27`, `Constants.java:208`).
- Public surface:
  - `setIndexerSpeed`, `startIndexer`, `stopIndexer` (`Indexer.java:34`).
  - `setHotdogSpeed`, `startHotdog`, `reverseHotdog`, `stopHotdog` (`Indexer.java:52`).
  - `getCommandedIndexerSpeed`, `getCommandedHotdogSpeed`, `isFeedingForward` (`Indexer.java:74`, `Indexer.java:78`, `Indexer.java:88`).
  - Command factories: `feedCommand`, `runEndHotdog(double)` (`Indexer.java:93`).
- Default command: none.
- Control mode: velocity control with `VelocityVoltage` (`Indexer.java:21`).
- Current gains and values:
  - Indexer speed `55 RPS`, kP `0`, kV `0.1285` (`Constants.java:212`, `Constants.java:219`).
  - Hotdog speed `70 RPS`, unjam `-50`, kP `0`, kV `0.11` (`Constants.java:213`, `Constants.java:222`).
- Limits/current:
  - Indexer stator 80 A, supply 60 A (`Constants.java:225`).
  - Hotdog stator 50 A, supply 30 A, lower limit 40 for 0.5 s (`Constants.java:234`).
- Gotcha: `isFeedingForward()` is used by drive to reduce teleop speed (`Drive.java:404`).

### Turret - `src/main/java/frc/robot/subsystems/turret/Turret.java`

- Instances: two `Turret` objects, `turret1` and `turret2` (`RobotContainer.java:147`, `RobotContainer.java:158`).
- Hardware per turret: one azimuth TalonFX, one leader flywheel TalonFX, one opposed follower TalonFX, one hood ServoHub channel, and shared CANdi zeroing device (`Turret.java:41`, `Flywheels.java:22`, `Hood.java:20`).
- Public surface:
  - `getRobotPose3d(Translation2d)` - AdvantageScope component pose (`Turret.java:63`).
  - `runSetpoints(Rotation2d, double, double)` - set azimuth/hood/flywheel with clamping/wrapping (`Turret.java:77`).
  - `runAutoTarget(ShootingParameters)` and `runZeroAzimuthTarget(ShootingParameters)` (`Turret.java:87`).
  - Static `toggleTurretMode()` / `getTargetingMode()` (`Turret.java:95`).
  - Logged accessors for commanded/measured azimuth, hood, flywheel, lock state (`Turret.java:107`).
- Default command: both turrets run `TurretTargeting` toward hub by default (`RobotContainer.java:384`).
- Internal state/control modes:
  - Static `lockedIn` affects both turrets (`Turret.java:34`).
  - Azimuth uses Motion Magic through `Azimuth.setTargetAngleDeg` (`Azimuth.java:23`).
  - Hood uses open-loop pulse width (`Hood.java:26`).
  - Flywheel uses velocity control and follower opposed to leader (`Flywheels.java:28`, `Flywheels.java:31`).
  - Zeroing is disabled-only and edge-triggered on CANdi switch close (`Turret.java:143`, `Turret.java:151`).
- Current gains and values:
  - Azimuth gear ratio `10`, kP `50`, kS `0`, kV `0.12`, Motion Magic velocity `24`, accel `48` (`Constants.java:72`, `Constants.java:96`).
  - Flywheel gear ratio `1.0`, kP `1.2`, kD `0.25`, kV `0.0925` (`Constants.java:73`, `Constants.java:103`).
  - Hood min/max `13` to `29.85` deg (`Constants.java:81`).
- Limits/current:
  - Left azimuth: min `-380`, max `1`; right azimuth: min `-365`, max `10`; margin `10 deg` (`Constants.java:75`).
  - Azimuth current: stator 60 A, supply 25 A (`Constants.java:152`).
  - Flywheel current: stator 60 A and 0.1 s closed-loop voltage ramp (`Constants.java:123`).
- Gotchas:
  - Comments call turret limits placeholders (`Constants.java:75`).
  - Commented default-targeting alternatives in `RobotContainer` mention "Re-enable hub tracking" but current live code already uses hub default targeting; read this block carefully before changing lock behavior (`RobotContainer.java:376`).

### Vision - `src/main/java/frc/robot/subsystems/vision/Vision.java`

- Hardware: real mode constructs two `VisionIOLimelight`s named `limelight-back` and `limelight-right` (`RobotContainer.java:105`, `VisionConstants.java:41`).
- Public surface:
  - `getTargetX(int)` returns latest `tx` for simple servoing (`Vision.java:59`).
  - Pose measurements are delivered to a `VisionConsumer`, wired to `Drive.addVisionMeasurement` (`RobotContainer.java:105`, `Vision.java:175`).
- Default command: none.
- Control modes: multi-camera pose filtering in `periodic()`, then accepted observations are sent to drive pose estimator (`Vision.java:64`, `Vision.java:140`).
- Current constants:
  - AprilTag layout: `AprilTagFields.kDefaultField` (`VisionConstants.java:17`).
  - Reject ambiguity over `0.3`, Z error over `0.75 m` (`VisionConstants.java:21`).
  - Baseline std devs: linear `0.02 m`, angular `0.06 rad` (`VisionConstants.java:44`).
  - MegaTag2 factors: linear `0.5`, angular `Infinity` (`VisionConstants.java:57`).
- Gotchas:
  - Real Limelight code publishes robot orientation to `robot_orientation_set` every update (`VisionIOLimelight.java:66`).
  - Real IO reads both `botpose_wpiblue` and `botpose_orb_wpiblue` (`VisionIOLimelight.java:49`).
  - `NetworkTableInstance.getDefault().flush()` is called in real mode from vision periodic (`Vision.java:70`).

### Field2dPublisher - `src/main/java/frc/robot/util/Field2dPublisher.java`

- Purpose: publishes `drive::getPose` to a SmartDashboard `Field2d` key `"Field"` (`RobotContainer.java:170`, `Field2dPublisher.java:15`).
- Public surface: `getField()` (`Field2dPublisher.java:22`).
- Default command: none.

### TurretVisualization - `src/main/java/frc/robot/subsystems/turret/TurretVisualization.java`

- Purpose: logs `ZeroedComponentPoses` and `FinalComponentPoses` arrays for AdvantageScope robot components (`TurretVisualization.java:20`).
- Hardware: no direct hardware; reads both turret pose accessors.
- Default command: none.

## 5. COMMANDS & COMMAND COMPOSITIONS

### Command Classes / Factories

| Command | Purpose | Requirements | End condition |
|---|---|---|---|
| `DriveCommands.joystickDrive` | Field-relative teleop drive with squared joystick inputs (`DriveCommands.java:62`) | `drive` | Runs until interrupted |
| `DriveCommands.joystickDriveAtAngle` | Field-relative drive while PID-locking heading to supplied angle (`DriveCommands.java:99`) | `drive` | Runs until interrupted |
| `DriveCommands.feedforwardCharacterization` | Drive FF characterization, prints kS/kV when canceled (`DriveCommands.java:147`) | `drive` during run sections | Runs until canceled after ramp sequence starts |
| `DriveCommands.wheelRadiusCharacterization` | Spin to calculate wheel radius (`DriveCommands.java:206`) | `drive` | Runs until canceled |
| `IntakeCommand` | Extend intake and set roller speed to `mintakeSpeed + 3 * driveSpeed` (`IntakeCommand.java:27`) | `intake` | Always false; stops intake in `end` (`IntakeCommand.java:38`) |
| `TurretTargeting` | Calculate shot parameters and command one turret (`TurretTargeting.java:17`) | one `Turret` | Always false (`TurretTargeting.java:85`) |
| `FuelSimCommand` | Initialize, step, and stop fuel sim (`FuelSimCommand.java:5`) | none | Always false and runs disabled (`FuelSimCommand.java:28`) |

### Named Command Compositions Registered For PathPlanner

| Name | Maps to | Requirements |
|---|---|---|
| `Extend Intake` | `intake.extendCommand()` (`RobotContainer.java:199`) | intake |
| `Start Intake` | `intake.onIntake()` (`RobotContainer.java:200`) | intake |
| `Stop Intake` | `intake.offIntake()` (`RobotContainer.java:201`) | intake |
| `Intake While Held` | `Commands.startEnd(intake::startIntake, intake::stopIntake, intake)` (`RobotContainer.java:202`) | intake |
| `Start Hotdog` | run once `indexer.startHotdog` (`RobotContainer.java:204`) | indexer |
| `Stop Hotdog` | run once `indexer.stopHotdog` (`RobotContainer.java:205`) | indexer |
| `Start Shoot` | starts indexer, hotdog, and intake (`RobotContainer.java:207`) | indexer, intake |
| `End Shoot` | stops indexer, hotdog, and intake (`RobotContainer.java:217`) | indexer, intake |

### Competition Trigger Bindings

Driver controller:

| Input | Command / behavior |
|---|---|
| Left stick + right X | Default swerve drive (`RobotContainer.java:386`) |
| `Y` | Reset current pose rotation to zero while preserving translation (`RobotContainer.java:314`) |
| `A` held | Drive while facing hub target (`RobotContainer.java:320`) |

Operator controller:

| Input | Command / behavior |
|---|---|
| Left trigger > 0.5 held | `IntakeCommand`: extend and run intake (`RobotContainer.java:273`) |
| Right bumper press | Retract intake (`RobotContainer.java:298`) |
| Right trigger > 0.5 held | Start indexer + hotdog; stop both on release (`RobotContainer.java:300`) |
| `B` held | Reverse intake and hotdog for unjam (`RobotContainer.java:333`) |
| `A` press | Toggle shared turret targeting lock mode, debounced 0.25 s (`RobotContainer.java:348`) |
| Robot in neutral zone | While true, both turrets target closer ferry target instead of hub (`RobotContainer.java:283`) |

Crazy mode driver-only bindings:

| Input | Command / behavior |
|---|---|
| Left trigger > 0.5 held | Intake command (`CrazyModeBindings.java:36`) |
| Right bumper press | Retract intake (`CrazyModeBindings.java:61`) |
| Right trigger > 0.5 held | Start indexer + hotdog (`CrazyModeBindings.java:63`) |
| `Y` | Reset heading to zero (`CrazyModeBindings.java:77`) |
| `A` held | Drive while facing hub (`CrazyModeBindings.java:83`) |
| `B` held without Start | Reverse intake roller at -45 (`CrazyModeBindings.java:96`) |
| Start + `B` held | Reverse indexer and hotdog (`CrazyModeBindings.java:100`) |
| POV 180 | Toggle turret targeting lock mode (`CrazyModeBindings.java:114`) |

## 6. AUTONOMOUS ROUTINES

Auto chooser: `new LoggedDashboardChooser<>("Auto Choices: ", AutoBuilder.buildAutoChooser())` (`RobotContainer.java:228`). Default selection is whatever PathPlanner's `AutoBuilder.buildAutoChooser()` returns; TODO: confirm in dashboard/PathPlanner GUI because this repo does not explicitly set a default.

| Auto file | Sequence | Starting position | What it scores / does | Runtime |
|---|---|---|---|---|
| `BlueSeedAuto.auto` | `BlueSeedPath` only (`BlueSeedAuto.auto:8`) | Path starts at `(2.5, 4.025)`, rot 0 (`BlueSeedPath.path:5`, `BlueSeedPath.path:45`) | Seed/pose path only; no events (`BlueSeedPath.path:34`) | TODO: confirm |
| `RedSeedAuto.auto` | `RedSeedPath` only (`RedSeedAuto.auto:8`) | Path starts at `(14.0607, 4.041)`, rot 180 (`RedSeedPath.path:5`, `RedSeedPath.path:45`) | Seed/pose path only; no events (`RedSeedPath.path:34`) | TODO: confirm |
| `PLAYOFF LEFT BATMAN.auto` | Extend intake, wait 0.6, north neutral one cycle, wait 3.75, end shoot, north neutral two cycle, wait 5, end shoot (`PLAYOFF LEFT BATMAN.auto:10`, `PLAYOFF LEFT BATMAN.auto:16`, `PLAYOFF LEFT BATMAN.auto:22`, `PLAYOFF LEFT BATMAN.auto:34`, `PLAYOFF LEFT BATMAN.auto:58`) | Uses `NorthStoot` linked path starts around `(3.826, 7.419)` (`NorthStoot_NeutralOne.path:5`) | Two north-side neutral pickups/returns with early shoot event markers (`NeutralOne_NorthStoot.path:41`, `NeutralTwo_NorthStoot.path:36`) | Explicit waits 9.35 s plus path time |
| `RIGHT GOTHAM DOUBLE.auto` | Extend intake, wait 0.6, south neutral one cycle, wait 4, end shoot, south neutral two cycle, wait 5, end shoot (`RIGHT GOTHAM DOUBLE.auto:10`, `RIGHT GOTHAM DOUBLE.auto:16`, `RIGHT GOTHAM DOUBLE.auto:22`, `RIGHT GOTHAM DOUBLE.auto:34`, `RIGHT GOTHAM DOUBLE.auto:58`) | Uses `SouthStoot` linked path starts around `(3.827, 0.647)` (`SouthStoot_NeutralOne.path:5`) | Two south-side neutral pickups/returns with early shoot event markers (`NeutralOne_SouthStoot.path:36`, `NeutralTwo_SouthStoot.path:36`) | Explicit waits 9.6 s plus path time |
| `SweepyFerry.auto` | Extend intake, start intake, wait 0.5, then run `NorthStart_Sweepy` in parallel with delayed Start Shoot after 3 s (`SweepyFerry.auto:10`, `SweepyFerry.auto:16`, `SweepyFerry.auto:22`, `SweepyFerry.auto:36`, `SweepyFerry.auto:51`) | Starts near `NorthStoot` `(3.826, 7.419)` (`NorthStart_Sweepy.path:5`) | Sweep path and ferry/shoot after delay | Explicit waits 3.5 s plus path time |

PathPlanner paths currently deployed:

- Seed: `BlueSeedPath.path`, `RedSeedPath.path`.
- North cycles: `NorthStoot_NeutralOne.path`, `NeutralOne_NorthStoot.path`, `NorthStoot_NeutralTwo.path`, `NeutralTwo_NorthStoot.path`, `NorthStart_Sweepy.path`.
- South cycles: `SouthStoot_NeutralOne.path`, `NeutralOne_SouthStoot.path`, `SouthStoot_NeutralTwo.path`, `NeutralTwo_SouthStoot.path`.
- Event marker regression test ensures every path event marker has a command payload (`PathPlannerEventMarkerConfigTest.java:15`).

Auto-specific tuning notes:

- `Intake Zone` event markers trigger `Intake While Held` in outbound pickup paths.
- `Shoot Early` markers trigger `Start Shoot` in return paths.
- Recent history shows autos and markers have been fragile: commits `e3c38de`, `d41cbbf`, `bb8812a`, `eae1000`.

## 7. CONSTANTS & TUNING VALUES (CURRENT SNAPSHOT)

### Drivetrain

- Max speed at 12 V: `4.58 m/s` (`TunerConstants.java:104`).
- Reduced teleop speed while intaking/shooting: `2.5 m/s` (`Constants.java:55`).
- Max angular speed: calculated as max linear speed / drive base radius (`Drive.java:413`); with current TunerConstants radius this is about `11.46 rad/s`.
- Wheelbase: `21.75 in / 0.55245 m`, derived from module X positions (`TunerConstants.java:166`, `TunerConstants.java:188`).
- Track width: `22.75 in / 0.57785 m`, derived from module Y positions (`TunerConstants.java:167`, `TunerConstants.java:178`).
- Gyro: Pigeon2 ID 46, yaw set to 0 during `GyroIOPigeon2` construction (`GyroIOPigeon2.java:24`, `GyroIOPigeon2.java:38`).
- Drive inversions: left side false, right side true (`TunerConstants.java:114`).
- Steer motors inverted true for all modules; encoders not inverted (`TunerConstants.java:162`, `TunerConstants.java:173`, `TunerConstants.java:184`, `TunerConstants.java:195`).

### Module Offsets

| Module | Encoder offset |
|---|---:|
| Front left | `0.375244140625` rotations (`TunerConstants.java:162`) |
| Front right | `0.006591796875` rotations (`TunerConstants.java:173`) |
| Back left | `0.090087890625` rotations (`TunerConstants.java:184`) |
| Back right | `-0.348388671875` rotations (`TunerConstants.java:195`) |

### Vision

- Field layout: `AprilTagFields.kDefaultField` (`VisionConstants.java:18`).
- Camera names: `limelight-back`, `limelight-right` (`VisionConstants.java:41`).
- Rejection thresholds: tag count must be > 0, ambiguity <= `0.3` for single tag, abs Z <= `0.75 m`, and pose inside field bounds (`Vision.java:99`, `VisionConstants.java:21`).
- Standard deviations: `0.02 m` linear and `0.06 rad` angular baseline, scaled by distance squared / tag count (`VisionConstants.java:44`, `Vision.java:126`).
- MegaTag2: linear std-dev multiplier `0.5`, angular `Infinity` (`VisionConstants.java:57`).

### Field Coordinates

- Field dimensions in constants: width `8.0756125 m`, length `16.5411785 m` (`Constants.java:254`).
- Hub target: `(4.6256194, 4.0346376)` in blue-origin frame (`Constants.java:250`).
- Neutral zone X range: `5.1816` to `11.303` (`Constants.java:260`).
- Alliance convention: code flips X, Y, and rotation on red alliance (`AllianceFlipUtil.java:16`, `AllianceFlipUtil.java:28`).
- PathPlanner AutoBuilder uses red-alliance boolean from DriverStation (`Drive.java:137`).

## 8. DESIGN DECISIONS & PHILOSOPHY (PROJECT-SPECIFIC)

- IO pattern: drive and vision use AdvantageKit-style IO interfaces (`ModuleIO`, `GyroIO`, `VisionIO`), while intake/indexer/turret directly own hardware in their subsystem/wrapper classes (`ModuleIO.java:13`, `VisionIO.java:14`, `Intake.java:18`, `Turret.java:23`). New drivetrain/vision hardware should follow the existing IO split; simple mechanisms currently do not.
- Logging strategy:
  - Robot extends `LoggedRobot` and records build metadata (`Robot.java:26`, `Robot.java:31`).
  - Real mode logs WPILOG + NT4; sim logs NT4; replay reads/writes WPILOG (`Robot.java:46`).
  - Mechanism telemetry uses `@AutoLogOutput` on subsystem accessors (`Intake.java:60`, `Indexer.java:47`, `Turret.java:107`).
  - Field and component visualization are explicit subsystems (`Field2dPublisher.java:10`, `TurretVisualization.java:20`).
- Simulation:
  - `Constants.simMode = Mode.SIM` (`Constants.java:19`).
  - Sim drive uses `ModuleIOSim`, sim vision uses `VisionIOPhotonVisionSim`, and fuel simulation is scheduled in `Robot.simulationInit()` via `RobotContainer.getSimulationCommand()` (`RobotContainer.java:112`, `Robot.java:149`).
  - Sim GUI is disabled by default in Gradle for AdvantageKit replay compatibility (`build.gradle:106`).
- Dashboard/control philosophy:
  - Binding modes are a dashboard chooser and only applied while disabled (`RobotContainer.java:231`, `RobotContainer.java:268`).
  - Fuel sim has a dashboard reset command that ignores disable (`RobotContainer.java:191`).
- Project-specific naming:
  - "hotdog" means one of the feeder/indexer motors (`Constants.java:208`).
  - "stoot" appears in path names for shoot positions.
  - "ferry" means alternate turret target selected when robot is in neutral zone (`RobotContainer.java:283`, `Constants.java:271`).
- Patterns explicitly rejected/changed by history:
  - PathPlanner event triggers were removed from `RobotContainer` and moved into GUI path event markers per commit `d41cbbf`; current registered NamedCommands back that up (`RobotContainer.java:199`).
  - Turret zeroing was changed to disabled-only in commit `fc5d170` and current code (`Turret.java:143`).
  - Drive limiting was revised multiple times (`bb7564c`, `886e098`); current limiter is inside `Drive.getMaxLinearSpeedMetersPerSec()` (`Drive.java:404`).

## 9. IMPLEMENTED FEATURES (RUNNING CHANGELOG)

Chronological highlights from git history and current code:

- Dual turret aiming and shot calculation:
  - What: two turret assemblies target hub/ferry and use shot table interpolation plus moving-shot lookahead.
  - Files: `Turret.java`, `TurretTargeting.java`, `GetAdjustedShot.java`, `shot_table.json`.
  - Status: working in current code; constants still have TODO/placeholder comments.
  - Related history: `63da917`, `8b208a6`, `418859b`, `3bc5f8a`.
- Limelight vision and camera pose tuning:
  - What: two Limelight pose sources inject accepted measurements into drive pose estimator.
  - Files: `Vision.java`, `VisionConstants.java`, `VisionIOLimelight.java`, `Drive.java`.
  - Status: working by code path; right camera transform has mismatch with AdvantageScope config.
  - Related history: `1b78a07`, `8648992`, `3a24c4d`, `ed03254`.
- AdvantageScope robot models:
  - What: robot/component GLB assets and turret pose logging.
  - Files: `AScope Assets/Robot_Chuck Norris/config.json`, `TurretVisualization.java`.
  - Status: working by current logging code.
  - Related history: `1ccb48d`, `531583c`, `6370418`.
- Ferry targeting:
  - What: when robot X is inside neutral zone, turrets target closest ferry target.
  - Files: `Constants.java`, `RobotContainer.java`, `CrazyModeBindings.java`.
  - Status: working in both competition and crazy binding loops.
  - Related history: `39ea381`, `df077fd`.
- Fuel simulation:
  - What: simulated fuel field, intake storage, outtake, dual-turret shooting, hub score logging, dashboard reset.
  - Files: `FuelSim.java`, `FuelSimulationController.java`, `FuelLaunchCalculator.java`, `FuelSimCommand.java`, sim tests.
  - Status: tested by JUnit (`FuelSimulationControllerTest.java:24`, `FuelLaunchCalculatorTest.java:16`).
  - Related history: `b0f98f6`, `1e2bfb0`, `d2b3912`, `0872bf9`.
- Autos and event markers:
  - What: north/south double pickup autos, sweepy ferry, seed paths, named PathPlanner events.
  - Files: `src/main/deploy/pathplanner/autos`, `src/main/deploy/pathplanner/paths`, `RobotContainer.java`.
  - Status: WIP/competition-tuned; git history shows repeated fixes and renames.
  - Related history: `cbda327`, `e3c38de`, `d41cbbf`, `bb8812a`, `1fa284b`, `eae1000`.
- Intake/indexer controls:
  - What: operator intake, shoot, retract, unjam, and speed compensation based on drive speed.
  - Files: `Intake.java`, `Indexer.java`, `IntakeCommand.java`, `RobotContainer.java`.
  - Status: working in current bindings.
  - Related history: `0d6492b`, `2d5e3c6`, `8e1adb9`.
- Control mode chooser:
  - What: switch between competition and crazy bindings only while disabled.
  - Files: `RobotContainer.java`, `CrazyModeBindings.java`, `RobotContainerBindingModeTest.java`.
  - Status: tested.
  - Related history: `b0f5cdf`.
- Field widget:
  - What: SmartDashboard `Field2d` pose publisher.
  - Files: `Field2dPublisher.java`, `RobotContainer.java`.
  - Status: working by code path.
  - Related history: `9ad1428`.
- Current limiting / drive config patch:
  - What: TunerConstants base drive/steer configs carry current limits and ModuleIOTalonFX clones them before applying module-specific gains.
  - Files: `TunerConstants.java`, `ModuleIOTalonFX.java`.
  - Status: working in current code.
  - Related history: `d91e9f0`, `3bc5f8a`.

Open TODOs connected to features are listed in the next section.

## 10. KNOWN ISSUES, TODOS, AND GOTCHAS

Explicit TODO/FIXME/NOTE/HACK-style comments:

- `Constants.java:75` - turret limits comment says "Placeholders - UPDATE ME".
- `Constants.java:163` - intake positions comment says "UPDATE ME!!!".
- `Constants.java:304` - fuel sim turret launch height references AdvantageScope component zeroed position.
- `LocalADStarAK.java:25` - notes upstream gist source for local AD* adapter.
- `PhoenixOdometryThread.java:132` - notes default Phoenix timestamps are not compatible with WPILib because they use the system clock.

Intentional commented-out code blocks:

- `RobotContainer.java:376` - alternate turret default commands are commented with instructions about hub tracking/lock behavior. Treat this as intentional operator-mode tuning history.
- `Robot.java:83` and `Robot.java:94` - thread priority toggles are commented out.
- `Turret.java:37` - old Mechanism2d visualization fields are commented out.
- `FuelSim.java:137` - fuel floor-position clamp is commented out in physics update.

Known or likely gotchas from code/history:

- Vision transform mismatch for `limelight-right` between `VisionConstants.java:34` and AdvantageScope config (`AScope Assets/Robot_Chuck Norris/config.json:60`).
- Real Limelight camera poses must be updated separately from Java `VisionConstants`; changing Java transforms alone does not reconfigure the physical Limelights (`VisionConstants.java:26`).
- PathPlanner settings do not match drive code mass/MOI/module spacing exactly (`Drive.java:65`, `src/main/deploy/pathplanner/settings.json:17`).
- `.auto` files show `"version": "2025.0"` (`BlueSeedAuto.auto:2`) in a 2026 project; TODO: confirm format compatibility.
- `GetAdjustedShot.ShootingParameters.isValid()` prints every time invalid parameters are checked (`GetAdjustedShot.java:34`); if the shot table fails to load this may spam stdout.
- `Hood` has no feedback; `positionDeg` is just the commanded value (`Hood.java:17`, `Hood.java:33`).
- Both hood channels share one static `ServoHub`, and hood servos are configured to keep power while disabled (`Hood.java:13`, `Hood.java:59`).
- `Turret.lockedIn` is static, so one toggle affects both turret instances (`Turret.java:34`).
- `Drive.periodic()` stops all modules and clears swerve setpoint logs while disabled; if the gyro disconnects outside sim, odometry falls back to wheel-delta kinematics and raises an alert (`Drive.java:180`, `Drive.java:211`, `Drive.java:226`).
- Control mode changes only apply while disabled through `RobotContainer.checkMode()` before the scheduler runs; live mode switching while enabled is intentionally ignored (`Robot.java:80`, `RobotContainer.java:255`, `RobotContainer.java:268`).
- Tracked repo hygiene oddity: `.DS_Store` files and `.vscode/settings.json` are already committed even though `.gitignore` ignores them; do not add more ignored IDE/OS files (`.gitignore:7`, `.gitignore:31`).
- Past chat context is not present in the repo. Only one commit message mentions ChatGPT: `63a5a77 yayayyayayay chatgpt save me... SOTM hopefully...`.

## 11. BUILD, DEPLOY, AND SIMULATION (PROJECT-SPECIFIC ONLY)

- Canonical local commands:
  - `./gradlew build` - compile, run tests, generate `BuildConstants`, and run Spotless formatting via `compileJava` (`build.gradle:101`, `build.gradle:135`, `build.gradle:179`).
  - `./gradlew test` - run JUnit 5 tests (`build.gradle:94`, `build.gradle:101`).
  - `./gradlew simulateJava` - launch desktop sim; GUI is disabled by default unless enabled through WPILib sim tooling (`build.gradle:64`, `build.gradle:111`).
  - `./gradlew replayWatch` - run AdvantageKit ReplayWatch (`build.gradle:68`).
- Toolchain and dependency versions in this checkout:
  - GradleRIO `2026.2.1`, Java 17, JUnit Jupiter `5.10.1` (`build.gradle:3`, `build.gradle:8`, `build.gradle:94`).
  - Vendordeps: AdvantageKit `26.0.2`, PathPlannerLib `2026.1.2`, Phoenix 6 `26.1.3`, REVLib `2026.0.5`, WPILib-New-Commands `1.0.0`, PhotonLib `v2026.3.2` (`vendordeps/*.json`).
- Non-standard Gradle tasks/custom behavior:
  - `replayWatch` JavaExec task runs AdvantageKit ReplayWatch (`build.gradle:68`).
  - `eventDeploy` can run `git add -A` and create a commit when a deploy task is run on a branch beginning with `event`; do not use deploy tasks casually from Claude (`build.gradle:145`).
  - `compileJava` depends on generated `BuildConstants` and `spotlessApply`, so compile/build can modify formatting in Java, Gradle, JSON, Markdown, and `.gitignore` files (`build.gradle:135`, `build.gradle:179`).
  - Static deploy files are copied from `src/main/deploy` to `/home/lvuser/deploy`; `deleteOldFiles = false` (`build.gradle:46`).
- Deploy directory contents:
  - `shot_table.json` with distance/hood/flywheel/time-of-flight points from `1.3 m` through `6.0 m`; units are meters, degrees, RPS, and seconds (`src/main/deploy/shot_table.json:2`, `src/main/deploy/shot_table.json:10`, `src/main/deploy/shot_table.json:106`).
  - Shot table loading sorts rows by distance and returns an empty list on parse/load failure; invalid shot parameters can then spam stdout from `isValid()` (`Distancer.java:43`, `GetAdjustedShot.java:34`).
  - PathPlanner settings/navgrid/autos/paths live under `src/main/deploy/pathplanner/`; settings currently use robot width `0.889 m`, length `0.864 m`, mass `66.0 kg`, MOI `8.45`, max drive speed `3.886 m/s`, and Kraken X60 drive motors (`settings.json:2`, `settings.json:17`, `settings.json:22`).
- Simulation:
  - Desktop support is enabled (`build.gradle:64`).
  - Sim GUI disabled by default for AdvantageKit replay compatibility; Driver Station sim is enabled (`build.gradle:106`).
  - `Constants.simMode` controls sim vs replay when not on a roboRIO; real hardware always uses `Mode.REAL` (`Constants.java:19`).
  - Simulation command is scheduled in `Robot.simulationInit()` (`Robot.java:149`).
  - Sim reset dashboard command is `"Fuel Sim/Reset Field Fuel"` (`RobotContainer.java:191`).
  - Root sim files include `networktables.json`, `simgui.json`, `simgui-ds.json`, and CTRE sim `.dat` files under `ctre_sim/`; these are support artifacts, not robot source.
- CI/GitHub Actions workflows:
  - No `.github/workflows` directory exists in this checkout. The only `.github` file is `.github/copilot-instructions.md`.

## 12. CONVERSATION & DECISION HISTORY

Repo markdown/docs:

- No README file found.
- No `docs/`, `notes/`, `design-docs/`, `decisions/`, or ADR directory found.
- `.github/copilot-instructions.md` mirrors the global agent instructions and is intentionally not repeated here.
- License files only: `AdvantageKit-License.md`, `WPILib-License.md`.

Commit-message context worth preserving:

- `d91e9f0` - drive implementation configs were patched to base off TunerConstants current limits.
- `fc5d170` - turret zeroing only while disabled.
- `9ad1428` - Field widget added.
- `bb8812a` - fixed auto event markers.
- `886e098` - reduced speed when intaking or shooting.
- `2d5e3c6` - changed unjam bindings for operator.
- `d41cbbf` - removed PathPlanner event trigger from RobotContainer and moved events properly into GUI.
- `d2b3912` - fuel reset button added to dashboard.
- `df077fd` - alliance flip utility changed to dynamic use rather than one-time static use.
- `6370418` - turret visualization fixed through setpoint changes.
- `39ea381` - auto ferry mode.
- `b0f5cdf` - experimental crazy button loop.
- `63a5a77` - only traceable prior-chat hint: commit message says "chatgpt save me"; no actual chat transcript is in repo.

## 13. QUICK REFERENCE FOR CLAUDE CODE

- Always obey `agents.md` alongside this file. In particular, never modify files under `.gradle`, `.wpilib`, `.vscode`, `.github`, or `gradle` unless the user explicitly overrides that rule (`agents.md:19`).
- If asked to add a new subsystem, the pattern in this repo is:
  - For drive/vision-like hardware: create an IO interface with `@AutoLog` inputs plus real/sim implementations, instantiate in `RobotContainer` based on `Constants.currentMode` (`RobotContainer.java:93`, `ModuleIO.java:13`, `VisionIO.java:14`).
  - For simpler mechanisms: subsystem directly owns motor objects and exposes small action methods plus command factories as needed (`Intake.java:18`, `Indexer.java:16`).
  - Add constants in `Constants.SubsystemConstants` near the existing mechanism classes (`Constants.java:36`).
- If asked to add a new auto:
  - Register any needed NamedCommands in `RobotContainer` before `AutoBuilder.buildAutoChooser()` (`RobotContainer.java:199`, `RobotContainer.java:228`).
  - Add/edit PathPlanner `.auto` and `.path` files under `src/main/deploy/pathplanner/`.
  - Ensure event markers carry command payloads; existing test checks this (`PathPlannerEventMarkerConfigTest.java:15`).
- If asked to tune PID/feedforward:
  - Drive/steer gains and module offsets: `src/main/java/frc/robot/generated/TunerConstants.java`.
  - Intake/indexer/turret gains: `src/main/java/frc/robot/Constants.java`.
  - Shot distance table: `src/main/deploy/shot_table.json`.
  - Vision confidence/rejection: `src/main/java/frc/robot/subsystems/vision/VisionConstants.java`.
- Files to read first for most tasks:
  - `src/main/java/frc/robot/RobotContainer.java` - object graph, bindings, autos, mode split.
  - `src/main/java/frc/robot/Constants.java` - IDs, mechanism limits, field constants, sim constants.
  - `src/main/java/frc/robot/subsystems/drive/Drive.java` - drivetrain, pose estimator, PathPlanner integration.
  - `src/main/java/frc/robot/subsystems/turret/Turret.java` and `src/main/java/frc/robot/subsystems/turret/TurretTargeting.java` - shooter behavior.
  - `src/main/java/frc/robot/generated/TunerConstants.java` - actual swerve hardware geometry/config.
